#!/bin/bash

# TODO: create a script which takes a name of your project, renames all the files and replaces all the occurrences of the name in the files,
#       reinitializes the git repository, and then removes itself - NOTE: name can't be empty or contain "/" character

echo "NOT IMPLEMENTED"